name = "artist-proxy"
main = "og-worker.js"
compatibility_date = "2024-01-01"
account_id = "48c0f0149933a7f0e3a24479e33c69d3"

routes = [
  { pattern = "*.massivemedias.com/*", zone_name = "massivemedias.com" },
  { pattern = "massivemedias.com/*", zone_name = "massivemedias.com" }
]

# Cron trigger: ping le backend Render toutes les 5 minutes pour empecher
# qu'il s'endorme (Render free tier sleep apres 15min d'inactivite).
# Resultat: backend toujours eveille, aucun cold start pour les clients.
[triggers]
crons = ["*/5 * * * *"]
