name = "artist-proxy"
main = "og-worker.js"
compatibility_date = "2024-01-01"
account_id = "48c0f0149933a7f0e3a24479e33c69d3"

routes = [
  { pattern = "*.massivemedias.com/*", zone_name = "massivemedias.com" },
  { pattern = "massivemedias.com/*", zone_name = "massivemedias.com" }
]

# Cron keep-alive retire: backend Render est maintenant sur plan payant "Always On"
# (pas de sleep), plus besoin de pinger. La DB Neon est aussi "Always On".
# Pour reactiver un cron Worker (ex: autre tache periodique), decommenter:
# [triggers]
# crons = ["*/5 * * * *"]
