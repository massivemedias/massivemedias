{
  "kind": "collectionType",
  "collectionName": "inventory_items",
  "info": {
    "singularName": "inventory-item",
    "pluralName": "inventory-items",
    "displayName": "Inventaire",
    "description": "Gestion d'inventaire — textiles, cadres, accessoires (hors consommables)"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "nameFr": {
      "type": "string",
      "required": true
    },
    "nameEn": {
      "type": "string",
      "required": true
    },
    "sku": {
      "type": "string",
      "unique": true
    },
    "category": {
      "type": "enumeration",
      "enum": ["textile", "frame", "accessory", "other"],
      "required": true
    },
    "variant": {
      "type": "string"
    },
    "quantity": {
      "type": "integer",
      "required": true,
      "default": 0
    },
    "lowStockThreshold": {
      "type": "integer",
      "default": 5
    },
    "costPrice": {
      "type": "decimal"
    },
    "image": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"]
    },
    "notes": {
      "type": "text"
    },
    "active": {
      "type": "boolean",
      "default": true
    }
  }
}
