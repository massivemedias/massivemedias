/**
 * Donnees locales des tatoueurs (fallback si CMS indisponible).
 * Structure identique a artists.js pour la coherence.
 *
 * Note: la section Tatoueurs est actuellement desactivee dans la navigation
 * principale. Les pages /tatoueurs restent accessibles en direct si besoin
 * mais ne sont plus mises en avant. Les donnees seront restaurees quand
 * la section sera reintroduite.
 */
const tatoueurs = {};

export default tatoueurs;
