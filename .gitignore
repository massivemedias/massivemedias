# Dependencies
node_modules/

# Build outputs
# backend/dist n'est PAS commite : Render recompile depuis src a chaque deploy
# (render.yaml -> buildCommand: npm install && npm run build). Le dist commite
# etait ignore par la prod (mythe "prod = dist commite" infirme le 12 juillet 2026,
# preuves : order.ts require invoice-counter jamais dans dist + API order live).
dist/
build/
.vite/
*.local

# Environment
.env
.env.local
.env.*.local

# Strapi
.cache/
.strapi/
*.db
*.db-*

# Old site backup
_old/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Coverage
coverage/

# Large media files (videos)
*.mp4
*.mov
*.avi
*.flv
*.wmv
*.MOV

# Backend (not deployed to GitHub Pages)
backend/node_modules/
backend/.cache/
backend/.strapi/
backend/build/
backend/*.db
backend/*.db-*

# Scripts et outils temporaires
seed_strapi.py
cloudflare-worker/.wrangler/

# Images originales (garder seulement les WebP dans frontend/public)
images/

# --- SHIELD ANTI-ICLOUD ZOMBIES ---
*tatoueur*
*Tatoueur*
*flash-tattoo*
*FlashTattoo*
* 2/
* 3/
* [0-9].*
# ----------------------------------
